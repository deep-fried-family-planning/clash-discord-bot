import * as E from 'effect/Effect';
import * as Q from 'effect/MutableQueue';
import * as Queue from 'effect/TPriorityQueue';
import * as S from 'effect/MutableList';

const bfs = () => {
  const queue = Q.unbounded();
};

Q.unbounded();
