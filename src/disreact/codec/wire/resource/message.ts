import {Struct} from 'effect/Schema';



export const BaseMessage = Struct({

});



export const BaseMessageInteractionMetadata = Struct({});

export const MessageCommandInteractionMetadata = Struct({});

export const MessageComponentInteractionMetadata = Struct({});

export const MessageModalSubmitInteractionMetadata = Struct({});

export const CommandInteractionMessage = Struct({

});

export const MessageComponentInteractionMessage = Struct({

});

export const ModalSubmitInteractionMessage = Struct({

});



export const MessageCallbackData = Struct({

});

export const WebhookMessage = Struct({

});
