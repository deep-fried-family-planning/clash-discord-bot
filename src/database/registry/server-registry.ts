import {Data} from 'effect';

export * as ServerRegistry from 'src/database/registry/user-registry.ts';
export type ServerRegistry = never;

export const get = () => {};

export const scanAll = () => {};

export const register = () => {};

export const update = () => {};
