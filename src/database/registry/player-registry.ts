import {Data} from 'effect';

export * as PlayerRegistry from 'src/database/registry/player-registry.ts';
export type PlayerRegistry = never;

export const get = () => {};

export const query = () => {};

export const scanAll = () => {};

export const register = () => {};

export const update = () => {};
